    echo "${helpComment} images will be smoothed  : ${gaussianKernel}"
    echo "${helpComment} volumes to be smoothed   : $volumeWILD"
    echo "${helpComment} prepending name          : $outputName"
    echo 

    . ${thisDir}/auxiliary/help/help_options_end
